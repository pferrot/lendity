<ui:composition template="/WEB-INF/jsp/inc-templates/main-template.jspx"
	xmlns="http://www.w3.org/1999/xhtml"
	xmlns:ui="http://java.sun.com/jsf/facelets"
	xmlns:f="http://java.sun.com/jsf/core"
	xmlns:h="http://java.sun.com/jsf/html"
    xmlns:c="http://java.sun.com/jstl/core_rt"
	xmlns:o="http://myfaces.apache.org/orchestra">

	<!-- params for page titles-->
	<ui:param name="pageTitle" value="Person Search" />
	<ui:param name="contentHeader" value="Person Search" />

	<ui:define name="content">

		<ui:decorate template="/WEB-INF/jsp/inc-templates/content-template.jspx">

			<!-- header content here  -->
			<ui:define name="pageName">
            	#{contentHeader}
          	</ui:define>

			<!-- insert content here -->
			<ui:define name="pageContent">
<!-- 				<ui:include src="/WEB-INF/inc-content/component-content.jspx" /> -->
				<f:view>
					<center>

<h:form>
	<h:inputText id="searchString" value="#{personsListController.searchString}"/>
	<br/>
	<h:commandButton value="#{messages.search}" action="#{personsListController.search}" />
	<h:commandButton value="#{messages.clearSearch}" action="#{personsListController.clearSearch}" />
</h:form>
<br/>

<h:dataTable value="#{personsListController.list}" binding="#{personsListController.table}" var="person">

	<h:column>
        <f:facet name="header">
            <h:outputText value="ID" />
        </f:facet>
        <h:outputText value="#{person.id}" />
    </h:column>

	<h:column>
        <f:facet name="header">
            <h:outputText value="FIRST NAME" />
        </f:facet>
		<o:separateConversationContext>
        	<h:outputLink value="#{personsListController.personOverviewHref}">
            	<h:outputText value="#{person.firstName}" />
        	</h:outputLink>
		</o:separateConversationContext>
    </h:column>
    
	<h:column>
        <f:facet name="header">
            <h:outputText value="REQUEST CONNECTION" />
        </f:facet>
		<o:separateConversationContext>
			<h:commandButton action="#{personsListController.requestConnection}" value="#{personsListController.requestConnectionLabel}"/>
		</o:separateConversationContext>
    </h:column>    

</h:dataTable>

<br/>

<h:form>
    <h:commandLink rendered="#{personsListController.previousPage}" value="#{messages.previousPage}" action="#{personsListController.previousPage}"/>
    <h:commandLink rendered="#{personsListController.nextPage}" value="#{messages.nextPage}" action="#{personsListController.nextPage}"/>
</h:form>

					</center>
				</f:view>
			</ui:define>
		</ui:decorate>

	</ui:define>
</ui:composition>

